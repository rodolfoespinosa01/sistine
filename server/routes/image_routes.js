//POST: Add a new image

//GET(/:id): Get a particular image in the database
//GET(/user/:user_id): Get all images uploaded by user
//GET(/product/:product_id): Get all images associated with a product

//DELETE: First, replace all instances of an image link with a "removed" image, and then delete the image from the database